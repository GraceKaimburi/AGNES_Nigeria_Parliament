import { agnesChatbot } from './AGNESChatbot.js';

// Initialize the chatbot when the DOM is fully loaded
document.addEventListener('DOMContentLoaded', () => {
  // Initialize chatbot
  agnesChatbot.init();
});